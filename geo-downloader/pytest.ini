[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    p0: 核心管道 - 必须通过
    p1: 后处理管道 - 需要rasterio
    p2: 下载器 - 全Mock
    p3: UI一致性 - HTML解析
    slow: 慢速测试
    integration: 集成测试
